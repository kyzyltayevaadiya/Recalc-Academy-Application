# See AGENTS.md

Follow the instructions in `AGENTS.md`.

# Pending: restore real git history (2026-09-20)

This repo's `.git` currently has only a fake 2-commit squashed history. The
real history (7 commits, e.g. "Transform site into a scroll-driven cinematic
RPG") still lives in `/Users/adiyakyzyltayeva/Desktop/base44-project-6aaf7af1/.git`
— see the full plan and context written there in that folder's `CLAUDE.md`.
The user wants that real history restored here before anything else git-related.
